# ===============================
# ⚙️ CONFIGURATION BASE DE DONNÉES
# ===============================
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/bqxx8drhv8wljcabp73b?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8mb4
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Pool de connexions Hikari
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.max-lifetime=30000
spring.datasource.hikari.connection-timeout=20000

# ===============================
# ⚙️ CONFIGURATION JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.connection.useUnicode=true
spring.jpa.properties.hibernate.connection.characterEncoding=utf8mb4
spring.jpa.properties.hibernate.connection.charSet=utf8mb4
spring.jpa.properties.hibernate.connection.collation=utf8mb4_unicode_ci

# Encodage UTF-8 (HTTP + requêtes)
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# ===============================
# ⚙️ APPLICATION
# ===============================
spring.application.name=fasocarbu
server.port=9090

# ===============================
# 🔐 JWT
# ===============================
fasocarbu.jwtSecret=VotreCleSecreteSuperSecureDauMoins32Caracteres
fasocarbu.jwtExpirationMs=86400000

# ===============================
# 📧 CONFIGURATION EMAIL (GMAIL)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=diarrassoubaa505@gmail.com
spring.mail.password=ucyi vizo iltn zqnp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.default-encoding=UTF-8

# ===============================
# 🪵 LOGGING
# ===============================
logging.level.org.springframework.security=DEBUG
logging.level.com.fasocarbu=DEBUG
logging.file.name=application.log
